jtype: Agent
with:
  id: 'multi_modal_agent'
  profile: 'helpful assistant that can generate images.'
  goal: "Choose and Take actions according to human's input."
  actions: ['DrawImage']
  watch: ['StoryBoard']
py_modules:
  - base_agent.py

